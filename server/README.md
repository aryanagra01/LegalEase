# Ms LegalEase Server (v3)

- Persona: **Ms LegalEase**
- Lenient inference + capped re-asks (3) per field
- Early confirm when coverage is high

Env:
```
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
PORT=3001
ALLOWED_ORIGIN=http://localhost:5173
```
